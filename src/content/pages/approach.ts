import type { ApproachPageContent } from "../schemas";
import data from "../../../content/pages/approach.json";

/** Approach page copy. Editable via /admin. See src/content/pages/home.ts. */
export const approachContent: ApproachPageContent = data;
