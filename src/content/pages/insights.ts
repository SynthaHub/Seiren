import type { InsightsPageContent } from "../schemas";
import data from "../../../content/pages/insights.json";

/** Insights page copy. Editable via /admin. See src/content/pages/home.ts. */
export const insightsContent: InsightsPageContent = data;
