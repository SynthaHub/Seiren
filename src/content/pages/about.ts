import type { AboutPageContent } from "../schemas";
import data from "../../../content/pages/about.json";

/** About page copy. Editable via /admin. See src/content/pages/home.ts. */
export const aboutContent: AboutPageContent = data;
