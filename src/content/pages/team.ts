import type { TeamPageContent } from "../schemas";
import data from "../../../content/pages/team.json";

/** Team page copy, including the founder bio. Editable via /admin. */
export const teamContent: TeamPageContent = data;
