import type { ContactPageContent } from "../schemas";
import data from "../../../content/pages/contact.json";

/** Contact page copy. Editable via /admin. See src/content/pages/home.ts. */
export const contactContent: ContactPageContent = data;
